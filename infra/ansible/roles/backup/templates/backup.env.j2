AWS_ACCESS_KEY_ID={{ backup_access_key }}
AWS_SECRET_ACCESS_KEY={{ backup_secret_key }}
AGE_PUBLIC_KEY={{ backup_age_public_key }}
BACKUP_BUCKET=yantech-verbatim-backups
# Dead-man's-switch ping URL (healthchecks.io). Empty until provisioned: the
# backup then pings nothing and nothing breaks. See docs/security-review.md, D3.
BACKUP_PING_URL={{ backup_ping_url | default('') }}
